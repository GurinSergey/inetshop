export class ComplexCompareStorage {
  compare: Object;
  note: string;
  count: number ;

  constructor(compare: Object, note: string, count: number) {
    this.compare = compare;
    this.note = note;
    this.count = count;
  }
}
